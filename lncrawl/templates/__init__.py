"""Shared site templates combining soup and browser behaviors.

Each template provides parsing logic for a family of sites with similar
structure (e.g., Madara, MangaStream, NovelPub). Concrete crawlers inherit
from these to avoid duplication.
"""

